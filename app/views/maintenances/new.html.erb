<div class="container">
  <div class="row mt-10 rounded">
    <div class="col-6 m-auto mt-5 w-50 rounded p-5 border bg-transparent">
      <h1 class="mb-10">Déclarer une intervention</h1>
      <%= simple_form_for [ @equipment, @maintenance ] do |f| %>
        <%= f.input :title, label: 'Nom' %>
        <%= f.input :description %>
        <%= f.input :date, as: :date, html5: true %>
        <%= select_tag :external_interlocutor_id, options_for_select(@external_interlocutors.map{ |e| ["#{e.company} - #{e.name}", e.id] }), prompt: "Prestataires" %>
        <%= select_tag :internal_interlocutor_id, options_for_select(@internal_interlocutors.map{ |e| [e.visa, e.id] }), prompt: "Personnel Bioprotec" %>
        <%= f.input :status, label: 'Statut' %>
        <%= f.submit "Valider", class: "btn btn-success" %>
        <%= link_to "Annuler", root_path, class: "btn btn-danger" %>
      <% end %>
    </div>
  </div>
</div>
